# Environments
.venv/
__pycache__/
*.pyc

# Secrets — never commit this
.env

# Vector DB — regenerated by ingest.py in ~2-3 min, don't commit the binary index
chroma_db/

# Raw dataset — too large for GitHub, mention the Kaggle source in README instead
Dataset/twcs.csv

# Jupyter
.ipynb_checkpoints/